// Update SITE_URL once you know the final domain (Netlify subdomain or a
// custom domain). It's used to build canonical URLs, Open Graph tags, and
// the sitemap. Nothing breaks if you forget — search engines just won't get
// fully-qualified URLs until this is set correctly.
export const SITE_URL = "https://the-green-corner.netlify.app";
export const SITE_NAME = "The Green Corner";
export const DEFAULT_OG_IMAGE = "/images/logo.png";
